spring.h2.console.enabled=true
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_ON_EXIT=FALSE;NON_KEYWORDS=user
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configuraciones JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Swagger
#swagger.environment.url = /
#swagger.schemas=http
#swagger.paths=/

#SWAGGER_HOST en consul (se debe incluir la property dentro del config de cada entorno)
//swagger.host = localhost:8080

# Deploy
server.port=8080